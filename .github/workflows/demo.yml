name: GitHub Actions Demo
on: [push]
jobs:
  build_resume:
    runs-on: ubuntu-latest
    container: 
      image: kannkyo/pandoc-wkhtmltopdf
      volumes:
        - ${{ github.workspace }}:/build
    steps:
      - run: ls
      - run: pandoc --help
