name: GitHub Actions Demo
on: [push]
jobs:
  build_resume:
    runs-on: ubuntu-latest
    container: 
      image: kannkyo/pandoc-wkhtmltopdf
      volumes:
        - $GITHUB_WORKSPACE:/build
    steps:
      - run: ls
      - run: pandoc --help
